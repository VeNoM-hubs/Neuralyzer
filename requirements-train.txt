# Training dependencies (install on Google Colab).
# Colab already ships a compatible torch/torchaudio build with CUDA, so do NOT
# pip-install torch there -- only add the rest.

torch>=2.0.0
torchaudio>=2.0.0

# Pretrained HuBERT / wav2vec2 / XLS-R / BERT
transformers>=4.40.0

# Metrics + stratified train/val split
scikit-learn>=1.3.0
numpy>=1.24.0

# Config + progress
PyYAML>=6.0
tqdm>=4.65.0

# Audio I/O backend for torchaudio (wav loading)
soundfile>=0.12.1
